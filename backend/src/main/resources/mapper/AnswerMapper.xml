<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qa.mapper.AnswerMapper">

    <!-- 按问题ID查询回答，按创建时间排序 -->
    <select id="selectByQuestionIdOrderByCreatedAt" resultType="Answer">
        SELECT * FROM answers
        WHERE question_id = #{questionId}
        ORDER BY created_at DESC
    </select>

    <!-- 查询问题的已采纳回答 -->
    <select id="selectAcceptedByQuestionId" resultType="Answer">
        SELECT * FROM answers
        WHERE question_id = #{questionId} AND is_accepted = TRUE
    </select>

    <!-- 按作者查询回答 -->
    <select id="selectByAuthorId" resultType="Answer">
        SELECT * FROM answers
        WHERE author_id = #{authorId}
        ORDER BY created_at DESC
    </select>

</mapper>