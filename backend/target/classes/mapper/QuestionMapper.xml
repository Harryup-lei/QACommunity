<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qa.mapper.QuestionMapper">

    <!-- 未解决的问题，按创建时间排序 -->
    <select id="selectUnsolvedOrderByCreatedAt" resultType="Question">
        SELECT * FROM questions
        WHERE is_solved = FALSE
        ORDER BY created_at DESC
    </select>

    <!-- 未解决的问题，按点赞数排序 -->
    <select id="selectUnsolvedOrderByVotes" resultType="Question">
        SELECT * FROM questions
        WHERE is_solved = FALSE
        ORDER BY votes DESC
    </select>

    <!-- 按标签搜索 -->
    <select id="selectByTagsContaining" resultType="Question">
        SELECT * FROM questions
        WHERE LOWER(tags) LIKE LOWER(CONCAT('%', #{tags}, '%'))
    </select>

    <!-- 搜索问题（全文检索） -->
    <select id="searchByKeyword" resultType="Question">
        SELECT * FROM questions
        WHERE MATCH(title, content) AGAINST(#{keyword} IN BOOLEAN MODE)
    </select>

    <!-- 按作者查询问题 -->
    <select id="selectByAuthorId" resultType="Question">
        SELECT * FROM questions
        WHERE author_id = #{authorId}
        ORDER BY created_at DESC
    </select>

    <!-- 按标签和状态查询 -->
    <select id="selectByTagsAndSolved" resultType="Question">
        SELECT * FROM questions
        WHERE LOWER(tags) LIKE LOWER(CONCAT('%', #{tags}, '%'))
        AND is_solved = #{isSolved}
    </select>

</mapper>